{
  "name": "action",
  "version": "1.0.0",
  "description": "### This action creates change history between project commits ```yml uses: archaic10/create-changelog@main with:     github-token: ${{ secrets.GITHUB_TOKEN }} ```",
  "main": "index.js",
  "keywords": [],
  "author": "archaic10",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "@octokit/core": "^3.5.1",
    "base-64": "^1.0.0",
    "child_process": "^1.0.2",
    "fs": "^0.0.1-security"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "auto-changelog": "^2.4.0"
  }
}
